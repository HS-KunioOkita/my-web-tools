# docker-compose の Postgres と一致させる
# DB は dashboard 自身のヘルスチェック用、および後続ツールのデータ用
DATABASE_URL=postgres://app:app@localhost:5433/app

# Postgres コンテナの初期化に使われる
POSTGRES_DB=app
POSTGRES_USER=app
POSTGRES_PASSWORD=app
